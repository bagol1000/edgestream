% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edgestream.R
\name{triangle_count}
\alias{triangle_count}
\title{Global triangle count}
\usage{
triangle_count(G)
}
\arguments{
\item{G}{A StreamGraph.}
}
\value{
Numeric; each triangle counted once.
}
\description{
Triangles are counted on the underlying undirected graph: edge direction
and reciprocal edges are ignored, so the count does not depend on the
order in which edges were added.
}
\examples{
G <- stream_graph()
for (e in list(c(0,1), c(1,2), c(0,2))) add_edge(G, e[1], e[2])
triangle_count(G)
}
